source "$(dirname "${BASH_SOURCE[0]}")/.env.common"

export ISTAROTH_DATABASE_URI="sqlite+aiosqlite:///tmp/istaroth.db"
export ISTAROTH_AVAILABLE_MODELS="all"

# Optional: pipeline model for RAG (default: gemini-2.5-flash-lite)
# export ISTAROTH_PIPELINE_MODEL="gemini-2.5-flash-lite"

# Optional: preprocessing model for query routing (default: gemini-2.5-flash-lite)
# export ISTAROTH_PREPROCESSING_MODEL="gemini-2.5-flash-lite"

# Optional: OpenAI API key for transcription
# export OPENAI_API_KEY="your-openai-api-key-here"
