export ISTAROTH_DOCUMENT_STORE_SET="CHS:/path/to/chs_checkpoint"
export GOOGLE_API_KEY="your-google-api-key-here"

# Optional: embedding device (default: cuda)
# export ISTAROTH_TRAINING_DEVICE="cpu"

# Optional: vector store type (default: chroma)
# export ISTAROTH_VECTOR_STORE="chroma"

# Optional: remote embedding service URL (skips local model loading)
# export ISTAROTH_EMBEDDING_SERVICE_URL="http://localhost:8001"

# Optional: external Chroma servers (format: LANG:host:port,...)
# export ISTAROTH_EXTERNAL_CHROMA_SERVERS="CHS:localhost:8001"

# Optional: query transformer (default: identity; options: identity, rewrite)
# export ISTAROTH_QUERY_TRANSFORMER="identity"

# Optional: reranker (default: rrf; options: rrf, cohere)
# export ISTAROTH_RERANKER="rrf"

# Optional: Cohere API key (needed when ISTAROTH_RERANKER=cohere)
# export CO_API_KEY="your-cohere-api-key-here"

# Optional: LangSmith tracing
# export LANGSMITH_API_KEY="your-langsmith-api-key"
# export LANGSMITH_PROJECT="your-langsmith-project-name"
# export LANGSMITH_TRACING="true"
# export LANGSMITH_ENDPOINT="https://api.smith.langchain.com"
