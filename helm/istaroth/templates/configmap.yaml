apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "istaroth.fullname" . }}-config
  labels:
    app.kubernetes.io/name: {{ include "istaroth.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    {{- with .Values.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
data:
  # Add any non-sensitive configuration data here
  # Example:
  # LOG_LEVEL: "INFO"
